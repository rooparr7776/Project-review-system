"const mongoose = require('mongoose'); const User = require('./models/User'); const Panel = require('./models/Panel'); const TeamPanelAssignment = require('./models/TeamPanelAssignment'); mongoose.connect('mongodb+srv://Suchitra:suchi3590@projectreviewcluster.v4ikhob.mongodb.net/test?retryWrites=true&w=majority&appName=ProjectReviewCluster').then(async () => { console.log('Connected to MongoDB'); const panelMembers = await User.find({ role: 'panel' }); console.log('Panel members:', panelMembers.length); const panels = await Panel.find(); console.log('Panels:', panels.length); const assignments = await TeamPanelAssignment.find(); console.log('Team assignments:', assignments.length); if (assignments.length > 0) { console.log('Sample assignment:', assignments[0]); } process.exit(0); }).catch(err => { console.error('Error:', err.message); process.exit(1); });" 
